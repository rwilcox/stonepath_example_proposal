Do you want to accept or reject this proposal, "<%= @proposal.title %>"?

<%= button_to("Accept Proposal", {:proposal_state => "accepted", :proposal => @proposal, :id => @task, :action => "mark_task_as_complete"}) %>

<%= button_to("Kickback Proposal", {:proposal_state => "kickback", :proposal => @proposal, :id => @task, :action => "mark_task_as_complete"}) %>
